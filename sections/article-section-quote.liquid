<div class="article-section-content-wrap article-section-quote">
  {% if section.settings.quote == blank %}
    Enter text...
  {% else %}
    {{ section.settings.quote }}
  {% endif %}
</div>

{% schema %}
{
  "name": "Quote",
  "tag": "section",
  "class": "article-section article-section-quote",
  "settings":[{
    "id":"quote",
    "label": "Quote",
    "type": "textarea"
  }],
  "presets": [
    {
      "name": "Quote"
    }
  ],
  "enabled_on": {
    "templates": ["article"]
  }
}
{% endschema %}